spring:
  application:
    name: fin-main-server
  
  profiles:
    active: local

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    com.fintech.server: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: local
  
  datasource:
    url: jdbc:mysql://localhost:3307/findb?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: Aa5051140
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

---
spring:
  config:
    activate:
      on-profile: prod
  
  datasource:
    url: jdbc:mysql://localhost:3306/fintech_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:fintech_user}
    password: ${DB_PASSWORD:fintech_password}
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false